{
  "name": "offline_express",
  "version": "1.0.0",
  "description": "Backend service using Express.js",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "echo \"Creating production build...\" && if not exist dist mkdir dist && copy server.js dist\\ && copy package.json dist\\ && if not exist dist\\controllers mkdir dist\\controllers && xcopy controllers dist\\controllers /s /e /y && if not exist dist\\routes mkdir dist\\routes && xcopy routes dist\\routes /s /e /y && if not exist dist\\config mkdir dist\\config && xcopy config dist\\config /s /e /y && if not exist dist\\models mkdir dist\\models && xcopy models dist\\models /s /e /y && if not exist dist\\services mkdir dist\\services && xcopy services dist\\services /s /e /y && if not exist dist\\utils mkdir dist\\utils && xcopy utils dist\\utils /s /e /y && if not exist dist\\data mkdir dist\\data",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "express",
    "backend",
    "api"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.11.0",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "morgan": "^1.10.0",
    "node-schedule": "^2.1.1",
    "os-utils": "^0.0.14",
    "sqlite3": "^5.1.7",
    "uuid": "^11.1.0",
    "winston": "^3.11.0",
    "winston-daily-rotate-file": "^4.7.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
